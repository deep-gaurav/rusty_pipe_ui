on:
  push:
    branches:
      - master

# on:
#   push:
#     branches:
#       - master

jobs:
  windows-lib:
    # name: release-mac_x86_64
    runs-on:  windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      # - name: Cache Cargo registry
      #   uses: actions/cache@v2
      #   with:
      #     path: ~/.cargo/registry
      #     key: ${{ runner.os }}-release-mac_x86_64-stable-cargo-registry-${{ hashFiles('**/Cargo.lock') }}
      # - name: Cache Cargo index
      #   uses: actions/cache@v2
      #   with:
      #     path: ~/.cargo/git
      #     key: ${{ runner.os }}-release-mac_x86_64-stable-cargo-index-${{ hashFiles('**/Cargo.lock') }}
      # - name: Cache Cargo build debug
      #   uses: actions/cache@v2
      #   with:
      #     path: target/debug
      #     key: ${{ runner.os }}-release-mac_x86_64-stable-debug-target-${{ hashFiles('**/Cargo.lock') }}
      # - name: Cache Cargo build release
        # uses: actions/cache@v2
        # with:
        #   path: target/release
        #   key: ${{ runner.os }}-release-mac_x86_64-stable-debug-target-${{ hashFiles('**/Cargo.lock') }}
      # - name: Change to native
      #   run: cd native/rustypipeuinative
      - name: Build project
        run: cargo build --release
      - name: list release files
        run: cd target/release
        run: ls
        
      - uses: actions/upload-artifact@v2
        with:
          name: windows-release
          path: librustypipeuinative.dll
      # - name: Set output
      #   id: vars
      #   run: echo ::set-output name=tag::${GITHUB_REF#refs/*/}
      # - name: Upload binary to release
      #   uses: svenstaro/upload-release-action@v1-release
      #   with:
      #     repo_token: ${{ secrets.GITHUB_TOKEN }}
      #     file: target/release/librustypipeuinative.dll
      #     asset_name: librustypipeuinative.dll
      #     tag: ${{ steps.vars.outputs.tag  }}
      #     overwrite: true